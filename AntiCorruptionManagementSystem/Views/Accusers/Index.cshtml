@model IEnumerable<AntiCorruptionManagementSystem.Models.Accuser>

@{
    ViewBag.Title = "Index";
}

<h2>মামলাকারীর তালিকা</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            মামলাকারীর নাম
        </th>
        <th>
            অ্যাকাউন্ট সচল ?
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Name)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.IsActive)
        </td>
        <td>
            @Html.ActionLink("সম্পাদন করুন", "Edit", new { id=item.Sl }) |
            @Html.ActionLink("বিবরন", "Details", new { id=item.Sl }) |
            @Html.ActionLink("মুছুন", "Delete", new { id=item.Sl })
        </td>
    </tr>
}

</table>
